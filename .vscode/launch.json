{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "strlen (GDB)",
      "type": "gdb",
      "request": "launch",
      "program": "${workspaceFolder}/a.out",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build",
      "stopAtEntry": true,
      "externalConsole": false
    }
  ]
}
